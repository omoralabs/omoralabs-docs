const config = {
  websiteName:
    "Building blocks for composable, modern finance analytics | Omora Docs",
  websiteUrl:
    process.env.NEXT_PUBLIC_WEBSITE_URL || "https://docs.omoralabs.com/",
  websiteDescription:
    "Building blocks for composable, modern finance analytics.",
};

export default config;
