{
  "table_name": "revenue_changes",
  "primary_key": ["date", "revenue_type_id", "value_type_id"],
  "fields": {
    "date": {
      "type": "DATE NOT NULL",
      "description": "Foreign key to dates(date)"
    },
    "revenue_type_id": {
      "type": "INTEGER NOT NULL",
      "description": "Foreign key to revenue_types(id)"
    },
    "value_type_id": {
      "type": "INTEGER NOT NULL",
      "description": "Foreign key to value_types(id)"
    },
    "nr_of_customers": {
      "type": "INTEGER NOT NULL",
      "description": "Number of customers related to the revenue change"
    },
    "value_per_customer": {
      "type": "DOUBLE NOT NULL",
      "description": "Value per customer related to the revenue change"
    }
  },
  "foreign_keys": [
    {
      "field": "date",
      "references": "dates(date)"
    },
    {
      "field": "revenue_type_id",
      "references": "revenue_types(id)"
    },
    {
      "field": "value_type_id",
      "references": "value_types(id)"
    }
  ]
}
