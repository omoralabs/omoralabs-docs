---
title: Periods
description: Periods dimension, with monthly periods as atomic units for reporting and analytics.
---

export const caption = "A list of Monthly Periods"
export const table_name = "periods"

## Semantic Contract
### Definitions

| Column | Description |
|--------|-------------|
| id | Unique identifier for the period. |
| period | Numeric representation of the period (e.g., YYYYMM, such as `202601` for January 2026). |
| date | Representative date for the period defined as the first day of the month. |

### Schema & Sample
  <RenderTable csvPath={`registry/samples/${table_name}/sample.csv`} caption={caption}>
      <ReadComponents filePath={`registry/components/dimensions/presentation/${table_name}.sql`}/>
  </RenderTable>

## Data Format

<TypeTableFromFile filePath={`registry/components/dimensions/${table_name}.json`} />

## Dependencies

- None.
