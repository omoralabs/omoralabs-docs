---
title: GL Accounts
description: A chart of accounts dimension, defining financial account hierarchy used for reporting and metrics.
---

export const caption = "A list of GL Accounts"
export const caption_segmented = "A list of GL Accounts segmented with business units"
export const table_name="gl_accounts"

## Semantic Contract
### Definitions
| Column | Description |
|--------|-------------|
| id | Unique identifier for the GL account. |
| name | Name of the GL account (e.g., Revenue, Cost of Goods Sold, Marketing Expense). |
| parent_gl | Self-reference to the GL account for hierarchy (nullable if top-level). |
| business_unit_id | Reference to the business unit (only if segmented; links to [Business Units](./business-units)). |

### Schema & Sample

<GLAccountsSelector
  segmented={
  <RenderTable csvPath="registry/samples/gl_accounts/segmented.csv" caption={caption_segmented}>
  <ReadComponents filePath={`registry/components/dimensions/presentation/${table_name}_segmented.sql`}/>
  </RenderTable>
  }
  simple={
  <RenderTable csvPath="registry/samples/gl_accounts/simple.csv" caption={caption}>
  <ReadComponents filePath={`registry/components/dimensions/presentation/${table_name}_simple.sql`}/>
  </RenderTable>
  }
/>

## Data Format

<GLAccountsSelector
  segmented={
    <TypeTableFromFile filePath={`registry/components/dimensions/${table_name}_segmented.json`} />
  }
  simple={
    <TypeTableFromFile filePath={`registry/components/dimensions/${table_name}_simple.json`} />
  }
/>

## Dependencies

- **[Business Units](./business-units)**: required when `is_segmented=true`.
