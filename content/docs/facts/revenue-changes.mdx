---
title: Revenue Changes
description: Revenue facts on changes, defining price and volume per date, value type, and revenue type.
---

export const caption = "A list of Revenue Changes"
export const table_name="revenue_changes"

## Facts
### Definitions

| Column | Description |
|--------|-------------|
| date | Date in YYYY-MM-DD format, reference to [Dates](semantic-layer/dates). |
| revenue_type_id | Type of revenue change (links to [Revenue Types](semantic-layer/revenue-types), e.g., New Booking, Upsell). |
| value_type_id | Type of value measured (links to [Value Types](semantic-layer/value-types), e.g., actuals, budget). |
| nr_of_customers | Volume variable with number of customers contributing to this revenue change. |
| value_per_customer | Pricing variable, with revenue per customer for this change. |

### Sample & Implementation

  <RenderTable csvPath={`registry/facts/${table_name}.csv`} caption={caption}>
      <ReadComponents filePath={`registry/facts/sqls/${table_name}.sql`}/>
  </RenderTable>

## Data Format

<TypeTableFromFile filePath={`registry/facts/${table_name}.json`} />

<Callout>
  The primary key is formed by constrainst on dates, revenue types, and value types: <br/>
  `PRIMARY KEY (date, revenue_type_id, value_type_id)`
</Callout>

## Dependencies

- **[Dates](/semantic-layers//dates)**
- **[Revenue Types](/semantic-layers//revenue-types)**
- **[Value Types](/semantic-layers//value-types)**
