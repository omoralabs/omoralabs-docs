---
title: Value Types
description: Value types dimension, definition of actual values and different budget dimensions.
---

export const caption = "A list of Value types"
export const table_name = "value_types"

  <RenderTable csvPath="registry/samples/value_types/sample.csv" caption={caption}>
      <ReadComponents filePath={`registry/components/implementations/${table_name}.py`}/>
  </RenderTable>

## Usage

<ReadComponents filePath={`registry/components/dimensions/presentation/${table_name}.sql`} />

## Data Format

<TypeTableFromFile filePath={`registry/components/dimensions/${table_name}.json`} />

## Dependencies

### Internal

- None.

### External

- None.

## Integration

This dimension defines and returns the SQL required to create the `gl_accounts` table. Table creation and lifecycle management are handled by the database [Methods](/components/database/methods) and [Schema Manager](/components/database/schema-manager).

This dimension is fully self-contained.
